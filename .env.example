DATABASE_URL="postgresql://postgres:postgres@localhost:5432/settlix"
SOLANA_RPC_URL=""
NEXT_PUBLIC_SOLANA_RPC_URL=""
NEXT_PUBLIC_SOLANA_NETWORK="mainnet-beta"
JUPITER_API_KEY=""
# Run `openssl rand -base64 32` to generate this value
AUTH_SECRET=""
NEXT_PUBLIC_SITE_URL=""
RESEND_API_KEY=""
# Subscription relayer — generate: solana-keygen new --outfile relayer.json
# Then: SUBSCRIPTION_RELAYER_KEYPAIR_JSON=$(cat relayer.json)
# Fund the relayer address with ~0.1 SOL for renewal tx fees; top up periodically.
SUBSCRIPTION_RELAYER_KEYPAIR_JSON=""
# Shared secret for POST /api/cron/process-renewals — generate: openssl rand -hex 32
CRON_SECRET=""